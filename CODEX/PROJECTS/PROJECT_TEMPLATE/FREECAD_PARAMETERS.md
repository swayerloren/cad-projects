# FreeCAD Parameters

Record FreeCAD sketches, constraints, named dimensions, bodies, workbenches, macro entry points, export settings, and STEP/STL outputs.

Use this file only when the project uses FreeCAD or has FreeCAD components.

