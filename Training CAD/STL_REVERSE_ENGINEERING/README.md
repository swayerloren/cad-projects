# STL Reverse Engineering

This folder stores reusable workflows for turning STL/STEP/reference assets into clean CAD.

Use it for:

- measuring source STLs
- separating functional dimensions from mesh artifacts
- deciding between OpenSCAD, FreeCAD, or hybrid rebuilds
- documenting visual and mechanical accuracy reviews
- preserving original references while rebuilding parametric models

